import numpy as np

from util import Number, random_matrix, matrix_partitions


def gauss_elimination(matrix: np.ndarray) -> np.ndarray:

    NW, NE, SW, SE = matrix_partitions(matrix)



if __name__ == '__main__':
    print('dupa')
